# <機能名>

<!-- Phase 1 (Intent) で起票するファイル。AI-DLC のすべての起点。
     Phase 2 (Inception) で plan.md を作る際の入力になる。
     後続フェーズで要件が変わったら intent.md を更新し、その変更点からフローを再進行する（別フォルダは作らない）。

     書き方の指針: ../references/phase-1-intent.md の以下セクションを参照
       - 「Intent に書かないもの（4 種）」
       - 「Description / Completion Criteria は汎用的に書く」
       - 「品質ゲートの書き方」
-->

## Description

<!-- 何を作るか。1〜3 段落で具体的に。

     SIRIUS は複数プロダクト共通基盤。汎用機能として書き、
     特定プロダクト固有の用語（プロダクト名、ゲーム内固有名詞、シーン名等）は使わない。
     プロダクト固有の文脈は Context の「背景となる依頼」に閉じる。

     実装方針（どう作るか）はここに書かない（plan.md の領域）。
-->

## Context

<!-- なぜ作るか、どんな制約があるか。以下のような観点を含める:

     ✅ 書く:
     - 配置先パッケージ（候補レベルで可。最終決定は plan.md）
     - 既存類似機構との関係
     - パフォーマンス制約（相対・定性的に。具体数値は plan.md）
     - 利用想定（誰がどう使うか）
     - 動的軸（後述のサブ項目参照）
     - 背景となる依頼（プロダクト固有の文脈・関連スレッドリンク等はここに閉じる）
     - そのトピック固有の過去 PR 教訓（CLAUDE.md にないもの）

     ❌ 書かない（詳細は ../references/phase-1-intent.md 参照）:
     - スケジュール（リリース日 / マイルストーン / 期限）
     - 後段で決まる数値プレースホルダ（「GPU 予算 X ms は plan.md に転記」など）
     - CLAUDE.md 既出ルール（.meta 編集禁止、サブモジュール非コミット、領域別ガイドライン参照等）
     - 具体検証手段の固有名（uloop-compile, uloop-run-tests 等）

     【動的軸】以下のいずれかを明記する（Phase 2 でインタフェース層を決める入力になる）:
     - 静的: Inspector / Volume Profile で事前設定、ランタイム変更なし
     - 半動的: シーン入退出やステート遷移で C# が切替（頻度: 数秒〜数分単位）
     - 完全動的: フレーム単位 / 入力単位で頻繁に切替

     半動的・完全動的の場合、切替経路（どのスクリプト / イベントが触るか）の概要も併記する。
     例: 「ヒント発動時に対象アクターの `UsePerCharacterOutline = true` を per-actor で切替」
-->

## Completion Criteria

<!-- 完了条件。SMAV (Specific / Measurable / Atomic / Verifiable) を満たすほど AI 自律性が上がる。
     成功ケース・失敗ケース・品質ゲートの 3 要素を書く。
     プロダクト固有用語は使わず、汎用機能として記述する。
-->

### ✅ 成功ケース

<!-- 達成すべき動作。例:
     - Timeline Clip でエフェクト配置・パラメータ調整可
     - Volume プロファイル切替可
     - オプション ON/OFF をランタイム切替可能で、切替時に副作用なし
-->

### ❌ 失敗ケース

<!-- 起きてはいけないこと、エラーハンドリング。例:
     - Clip 0 個でも例外を出さない
     - Timeline 削除時にリソースリークなし
     - 不正な Mixer 入力 (null 等) でクラッシュしない
     - オプション OFF なのに本機能由来の追加処理が発生しない
-->

### 🔒 品質ゲート

<!-- 検証カテゴリ + 客観メトリクスで書く。
     具体の検証手段（コマンド / スキル / 計測ツールの固有名）はここに書かず、Phase 2 で plan.md に回す。

     ✅ 検証カテゴリ + 客観メトリクスの例:
     - コンパイル時エラー 0 / 警告 0（新規警告を出さない）
     - 既存および新規の自動テスト（EditMode / PlayMode）が pass
     - ビジュアルリグレッションテストで既存シーンに差分なし
     - 自動レビュー（Copilot 等）・人手レビューを pass
     - 負荷ゲート: 追加ドローコール・SetPass・RT 確保が想定範囲内

     ❌ NG（具体手段の固有名は書かない）:
     - uloop-compile エラー 0  → 「コンパイル時エラー 0」
     - uloop-run-tests pass    → 「自動テスト pass」
     - 人手レビューの依頼手順  → 「人手レビュー pass」
     - .meta は AI 編集禁止     → CLAUDE.md 既出のため Intent に書かない

     末尾に下記の一文を明記することを推奨:
     > 具体の検証手段（コマンド / スキル / 計測ツール）は Phase 2 (Inception) で plan.md に記述する。
-->

<!-- ============================================================
     このフェーズ (Phase 1: Intent) で決めるもの・作るもの まとめ
     ============================================================

     ▼ 決めるもの（このテンプレの各セクションで確定させる）
       1. Description   … 何を作るか。汎用機能として、プロダクト固有用語を使わず記述。
                          実装方針（どう作るか）は書かない（plan.md の領域）。
       2. Context       … なぜ作るか・制約。次を含める:
                          - 配置先パッケージ（候補レベルで可、最終決定は plan.md）
                          - 既存類似機構との関係
                          - パフォーマンス制約（相対・定性的に。具体数値は plan.md）
                          - 利用想定（誰がどう使うか）
                          - ★動的軸（静的 / 半動的 / 完全動的）＝必須。Phase 2 の
                            インタフェース層決定の入力。半動的・完全動的なら切替経路も。
                          - 背景となる依頼（プロダクト固有文脈はここに閉じる）
                          - そのトピック固有で CLAUDE.md にない過去 PR 教訓
       3. Completion Criteria … 完了条件。SMAV を満たすほど AI 自律性が上がる。3 要素で書く:
                          - ✅ 成功ケース（達成すべき動作）
                          - ❌ 失敗ケース（起きてはいけないこと・エラーハンドリング）
                          - 🔒 品質ゲート（検証カテゴリ + 客観メトリクス。手段の固有名は書かない）

     ▼ 決めない／書かないもの（Intent に混入させない 4 種）
       - スケジュール（リリース日 / マイルストーン / 期限）
       - 後段で決まる数値プレースホルダ（GPU 予算 X ms 等）→ 相対・定性的に留める
       - CLAUDE.md 既出ルール（.meta 編集禁止 / サブモジュール非コミット等）
       - 具体の検証手段の固有名（uloop-compile, uloop-run-tests 等）→ plan.md へ

     ▼ 作るもの（成果物）
       - docs/ai-dlc/<date>-<topic-slug>/intent.md 一枚（本テンプレをコピーして記入）

     ※ 実装方針・UoW 分解・具体の検証手段は Phase 2 (Inception) の plan.md で決める。
-->
